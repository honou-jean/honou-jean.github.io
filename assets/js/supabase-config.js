/*
 * Public browser configuration for the optional teaching portal.
 *
 * Fill these values only after creating the Supabase project. A publishable
 * key (sb_publishable_...) or a legacy anon key is safe to expose when every
 * exposed table and Storage bucket is protected by Row Level Security.
 * Never put an sb_secret_... or legacy service_role key in this file.
 */
window.SUPABASE_CONFIG = Object.freeze({
  url: "",
  publishableKey: ""
});
